# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'Example' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Example
   pod 'OZLivenessSDK', :git => 'https://gitlab.com/oz-forensics/oz-liveness-ios.git', :tag => '8.11.0'

end

target 'HalykWidget' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HalykWidget
   pod 'OZLivenessSDK', :git => 'https://gitlab.com/oz-forensics/oz-liveness-ios.git', :tag => '8.11.0'

  target 'HalykWidgetTests' do
    # Pods for testing
  end

end
